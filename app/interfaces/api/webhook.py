"""Deprecated: use aiogram setup in app.interfaces.telegram.aiogram_app.

This file previously created aiogram Bot/Dispatcher for a different layout.
Left intentionally minimal to avoid confusion; FastAPI webhook uses the
dispatcher from aiogram_app via app.main.
"""
